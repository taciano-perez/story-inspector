runtime: java
env: flex
#service: storyinspector-api

runtime_config:
  jdk: openjdk8

env_variables:
  SPRING_PROFILES_ACTIVE: "gcp,mysql"

handlers:
  - url: /.*
    script: this field is required, but ignored

resources:
  cpu: 2
  memory_gb: 12
  disk_size_gb: 10

manual_scaling:
  instances: 1